import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

export default class LayoutEngine extends Component {
  render() {
    return (
      <View>
        <Text> Layout engine textInComponent </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
